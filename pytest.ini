[pytest]
minversion = 7.0
# Only run the new, deterministic integration suite in CI.
# The existing scripts in src/test are interactive/load tests and not pytest-stable.
testpaths =
    src/test/integration
python_files =
    test_*.py
addopts =
    -q
    -ra
    --strict-markers
