spring.application.name=hte
server.shutdown=graceful

# Path to the engine initialization JSON.
hte.config.path=assets/config.json

# Local DB file used to persist users/sessions/bots.
# If left as "data.json" and it doesn't exist, the server seeds it from "assets/data.json".
hte.db.path=assets/data.json