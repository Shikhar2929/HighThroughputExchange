package hte.matchingengine;

/**
 * Order side.
 *
 * <p>{@link #BID} represents buy-side intent (bids). {@link #ASK} represents sell-side intent
 * (asks).
 */
public enum Side {
    BID,
    ASK;
}
