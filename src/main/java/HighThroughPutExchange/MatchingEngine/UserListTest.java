package HighThroughPutExchange.MatchingEngine;

import static org.junit.Assert.*;

import org.junit.*;

public class UserListTest {

    /*
     * @Test public void testUserInitializationWithInfiniteBalance() { UserList
     * userList = new UserList(true, 100.0); // Position limit of 100.0 boolean
     * result = userList.initializeUser("user1");
     * assertTrue("User should be initialized successfully", result);
     * assertEquals("Balance should be 0 for infinite case", 0.0,
     * userList.getUserBalance("user1"), 0.01);
     * assertEquals("Position limit should allow max volume trading as per limit",
     * 100.0, userList.getValidVolume("user1", 1.0), 0.01); }
     *
     * @Test public void testBalanceAdjustmentWithInfiniteBalance() { UserList
     * userList = new UserList(true, 50.0); // Position limit of 50.0
     * userList.initializeUser("user2"); boolean adjustResult =
     * userList.adjustUserBalance("user2", 500.0); assertEquals(500.0,
     * userList.getUserBalance("user2"), 0.00001); double validVolume =
     * userList.getValidVolume("user2", 10.0);
     * assertEquals("Valid volume should be limited by position limit", 50.0,
     * validVolume, 0.01); }
     */
}
